machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t violinbeats/iot-fleet-management

test:
    override:
      - mocha simpleTest.js

deployment:
  hub:
    branch: master
    commands:
      - docker login -e vincentsu.violin@gmail.com -u violinbeats -p vincent
      - docker push violinbeats/iot-fleet-management
